package net.ilexiconn.jurassicraft.ai.test;

public class Status {

	/** TAMED represents whether the creature is tamed or not. */
	public static final int TAMED = (int) Math.pow(2, 0);
	/** FLYING represents whether the creature is flying. */
	public static final int FLYING = (int) Math.pow(2, 1);
	/** SWIMMING represents whether the creature is swimming. */
	public static final int SWIMMING = (int) Math.pow(2, 2);
	/** SITTING represents whether the creature is sleeping. */
	public static final int SITTING = (int) Math.pow(2, 3);
	/** SLEEPING represents whether the creature is sitting. */
	public static final int SLEEPING = (int) Math.pow(2, 4);
	/** HUNGRY represents whether the creature is hungry. */
	public static final int HUNGRY = (int) Math.pow(2, 5);
	/** THIRSTY represents whether the creature is thirsty. */
	public static final int THIRSTY = (int) Math.pow(2, 6);
	/** EATING represents whether the creature is eating. */
	public static final int EATING = (int) Math.pow(2, 7);
	/** DRINKING represents whether the creature is drinking. */
	public static final int DRINKING = (int) Math.pow(2, 8);
	/** SOCIALIZING represents whether the creature is socializing. */
	public static final int SOCIALIZING = (int) Math.pow(2, 9);
	/** INJURED represents whether the creature is injured. */
	public static final int INJURED = (int) Math.pow(2, 10);
	/** DEFENDING represents whether the creature is defending itself. */
	public static final int DEFENDING = (int) Math.pow(2, 11);
	/** FLEEING represents whether the creature is fleeing. */
	public static final int FLEEING = (int) Math.pow(2, 12);
	/** PLAYING represents whether the creature is playing. */
	public static final int PLAYING = (int) Math.pow(2, 13);
	/** STALKING represents whether the creature is stalking a prey. */
	public static final int STALKING = (int) Math.pow(2, 14);
	/** BREEDING represents whether the creature is breeding. */
	public static final int BREEDING = (int) Math.pow(2, 15);
	/** INLOVE represents whether the creature is in love. */
	public static final int INLOVE = (int) Math.pow(2, 16);

}
